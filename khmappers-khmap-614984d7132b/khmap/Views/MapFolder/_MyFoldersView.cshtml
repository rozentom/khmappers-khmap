@using Microsoft.AspNet.Identity;
@model IEnumerable<khmap.Models.MapFolder>
@using khmap.Models;
@Scripts.Render("~/bundles/jquery")
@*if (Model.Count() == 0)
    {
        <div class="alert alert-info">No folders to display</div>
    }
    else
    {*@
    <table class="table table-condensed table-responsive table-hover">
 
        @if (true)
        {
            int i = 0;
            if (ViewBag.prevFolder == null)
            {
                i++;
            }
            foreach (var item in Model)
            {
                string id = item.Id.ToString();
                if (i > 0)
                {
                <tr>
                    <td class="col-md-2">
                        @if (ViewBag.prevFolder != null)
                        {
                           <i>&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp</i>
                        }
                        <label id="@i.ToString()" style="display:none">@id</label>
                        <i id="@id" onmousedown="onClick(document.getElementById('@i.ToString()').innerText, event, @i)" class="fa fa-folder" aria-hidden="true"><button class="btn-link"> @item.Name</button></i>          @*this is the folder "button"*@
                    </td>
                    <td class="col-md-2">
                        

                        @Html.ActionLink(" ", "Details", "MapFolder", new { id = item.Id }, new { @class = "btn btn-info btn-xs glyphicon glyphicon-edit", title = "Details" })
                        @*@Html.ActionLink(" ", "LaunchMap5", "Map", new { id = item.Id.ToString() }, new { @class = "btn btn-success btn-xs glyphicon glyphicon-ok", title = "Load" })*@
                        @if (item.Creator.ToString() == User.Identity.GetUserId())
                        {
                            @Html.ActionLink(" ", "Delete", "MapFolder", new { id = item.Id }, new { @class = "btn btn-danger btn-xs glyphicon glyphicon-trash", title = "Delete" })
                        }
                    </td>
                </tr>
                }
                else
                {
                    <tr>
                        <td class="col-md-9">
                            <i id="@item.Id" class="fa fa-folder" aria-hidden="true">@Html.ActionLink(item.Name, "Index", "Home", new { id = item.Id.ToString(), ownedOrShared = "owned" }, new { })</i>          @*this is the folder "button"*@
                        </td>
                        <td class="col-md-2">
                            @Html.ActionLink(" ", "", "", new { id = item.Id }, new { @class = "btn btn-info btn-xs glyphicon glyphicon-edit", title = "Details" })
                            @*@Html.ActionLink(" ", "LaunchMap5", "Map", new { id = item.Id.ToString() }, new { @class = "btn btn-success btn-xs glyphicon glyphicon-ok", title = "Load" })*@
                            @if (item.Creator.ToString() == User.Identity.GetUserId())
                            {

                                @Html.ActionLink(" ", "Delete", "MapFolder", new { id = item.Id }, new { @class = "btn btn-danger btn-xs glyphicon glyphicon-trash", title = "Delete" })
                            }
                        </td>
                    </tr>
                }
                i++;
            }
        }
        @foreach (var map in ViewBag.maps)
        {
                <tr>
                    <td class="col-md-1">
                        @*<span class="glyphicon glyphicon-star"></span>*@
                        @if (ViewBag.prevFolder != null)
                        {
                            <i>&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp</i>
                        }
                        <i class="fa fa-sitemap">
                            @Html.ActionLink(((Map)map).Name, "LaunchMap5", "Map", new { id = map.Id, currentFolderID = "null" }, new { title = "Load " + map.Name })
                        </i>


                    <td class="col-md-2">
                        @Html.ActionLink(" ", "Index", "Map", new { id = map.Id }, new { @class = "btn btn-info btn-xs glyphicon glyphicon-edit", title = "Details" })
                        @*@Html.ActionLink(" ", "LaunchMap5", "Map", new { id = item.Id.ToString() }, new { @class = "btn btn-success btn-xs glyphicon glyphicon-ok", title = "Load" })*@
                        @if (((Map)map).Creator.ToString() == User.Identity.GetUserId())
                        {
                            @Html.ActionLink(" ", "Delete", "Map", new { id = map.Id , currFolderID = ViewBag.currFolderIDString }, new { @class = "btn btn-danger btn-xs glyphicon glyphicon-trash", title = "Delete" })
                        }
                    </td>
                </tr>
            
        }

    </table>
    
  <div id="menu">
    <span class="menuItem">Delete</span>
    <br />
    <span class="menuItem">Edit</span>
    <br />
    <span class="menuItem">Move</span>
</div>



@*}*@

<script>

 

    function onClick(id, event, num) {



        var element = document.getElementById(id);
        element.oncontextmenu = function () { return false;}
            switch (event.which) {
                case 1:
                    window.location.href = '@Url.Action("Index", "Home", new {id = "ID", ownedOrShared = "owned" })'.replace("ID", id);
                    break;
                case 2:
                    console.log('Middle Mouse button pressed.');
                    break;
                case 3:
                    $("#menu").css('left', element.offsetLeft+43+element.offsetWidth);
                    $("#menu").css('top', element.offsetTop+element.offsetHeight+num*30);
                    $("#menu").show();
                    break;
                default:
                    console.log('You have a strange Mouse!');
            }
    }

    $(document).ready(function(){
        $(document).click(function (e) {
            if (e.button == 0) {
                $("#menu").fadeOut(80);
            }
        })
    })


</script>


<style>
    .table td {
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        word-wrap: break-word;
    }

    .table {
        table-layout: fixed;
    }

    table tr {
        width: auto !important;
    }

    #menu
    {
        background-color:#CDCDCD;
        border:2px solid #6A6A6A;
        border-radius:5px;
        box-shadow:5px 5px 10px #474747;
        width:130px;
        padding-top:5px;
        padding-bottom:5px;
        display:none;
        position:absolute;
    }
    .menuItem
    {
        padding-left:5px;
        padding-right:5px;
        font-family:Arial,Arial, Helvetica, sans-serif;
    }


</style>